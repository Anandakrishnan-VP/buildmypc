{
  "name": "zeus-pc-builder",
  "version": "1.0.0",
  "private": true,
  "description": "PC Builder — Inventory & Quotation Software",
  "scripts": {
    "server": "npm --prefix server start",
    "client": "npm --prefix client run dev",
    "dev": "node -e \"const { spawn } = require('child_process'); spawn('npm', ['--prefix', 'server', 'start'], { stdio: 'inherit', shell: true }); spawn('npm', ['--prefix', 'client', 'run', 'dev'], { stdio: 'inherit', shell: true });\""
  }
}
